{
  "skill_name": "Dev10x:skill-audit",
  "eval_dimensions": [
    {
      "id": "path-resolution",
      "name": "Session Path Resolution",
      "description": "Correctly auto-resolves JSONL path from CWD encoding or explicit directory argument"
    },
    {
      "id": "confirmation-gate",
      "name": "Session Confirmation Gate",
      "description": "Step 1.1 confirmation gate uses AskUserQuestion tool with session ID and mtime displayed for user verification"
    },
    {
      "id": "audit-execution",
      "name": "Audit Execution",
      "description": "Runs skill-audit analysis on the confirmed session JSONL file"
    },
    {
      "id": "findings-output",
      "name": "Findings Output",
      "description": "Produces structured skill findings report with counts and recommendations"
    }
  ],
  "evals": [
    {
      "id": 1,
      "name": "confirmation-gate-uses-tool",
      "prompt": "/Dev10x:skill-audit",
      "description": "Tests that Step 1.1 confirmation gate uses AskUserQuestion tool (not plain text) when path is auto-resolved.",
      "setup": {
        "cwd": "a worktree with recent session JSONL files",
        "explicit_path": "not provided",
        "invocation": "/Dev10x:skill-audit"
      },
      "dimensions": ["confirmation-gate"],
      "assertions": [
        {
          "id": "gate1_uses_tool",
          "text": "Step 1.1 confirmation gate calls AskUserQuestion (not plain text)",
          "check": "tool_called",
          "tool": "AskUserQuestion",
          "signal": "AskUserQuestion is called at the confirmation gate"
        },
        {
          "id": "displays_session_id",
          "text": "Question displays the session ID extracted from the JSONL filename",
          "check": "output_contains",
          "signals": ["Session:", "uuid", "session-id"]
        },
        {
          "id": "displays_resolved_path",
          "text": "Question displays the resolved file path",
          "check": "output_contains",
          "signals": ["Path:", ".jsonl"]
        },
        {
          "id": "displays_mtime",
          "text": "Question displays the file's modification time",
          "check": "output_contains",
          "signals": ["Last modified:", "mtime", "ago"]
        },
        {
          "id": "option_count",
          "text": "Gate presents exactly 2 options",
          "check": "option_count",
          "count": 2,
          "signal": "Two options: proceed or provide path"
        },
        {
          "id": "option_labels_correct",
          "text": "Options have correct labels as documented",
          "check": "output_contains",
          "signals": [
            "Yes, audit this session",
            "No, let me provide the path"
          ]
        },
        {
          "id": "multiselect_false",
          "text": "Option is single-select (multiSelect: false)",
          "check": "behavioral",
          "assertion": "single_option_selection",
          "signal": "User can select only one option, not multiple"
        }
      ]
    },
    {
      "id": 2,
      "name": "explicit-path-skips-gate",
      "prompt": "/Dev10x:skill-audit /path/to/session.jsonl",
      "description": "Tests that Step 1.1 gate is skipped when an explicit JSONL path is provided.",
      "setup": {
        "explicit_path": "/path/to/session.jsonl",
        "invocation": "/Dev10x:skill-audit /path/to/session.jsonl"
      },
      "dimensions": ["confirmation-gate"],
      "assertions": [
        {
          "id": "gate1_skipped",
          "text": "Confirmation gate is skipped when explicit path provided",
          "check": "tool_not_called",
          "tool": "AskUserQuestion",
          "signal": "AskUserQuestion is not called; audit proceeds directly"
        }
      ]
    }
  ]
}
